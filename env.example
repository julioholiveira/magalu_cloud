# Exemplo de variáveis de ambiente para magalu_cloud
# Copie este arquivo para .env e ajuste os valores reais antes de usar
# Ex: cp env.example .env && edit .env

# Sua chave da API (x-api-key)
API_KEY=YOUR_API_KEY